# Progressive Web App (PWA)

* manifest.json
* service worker

## News Portal
It's alive demo of PWA. `http://localhost:5000`

## Running locally
```
# Install serve
npm install -g serve

# Run PWA, inside project folder
server
```

